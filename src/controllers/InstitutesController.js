const Institute = require("../models/Institute")
const msg = require("../middlewares/msg")

const InstitutesController = {
  PostCreatedInstitute(req, res) {},
}
module.exports = InstitutesController
